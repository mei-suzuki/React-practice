import React, { FC, useState } from 'react';
// import {useState} from 'react';

const Header: React.FC = () => {
    // ヒント: 左矢印ボタンの表示・非表示は次のようにuseStateを使う
    // const [isActive, setActive] = useState<boolean>(false)
    return (
        <div>
        </div>
    );
}

export default Header;